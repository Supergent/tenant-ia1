# =============================================================================
# CONVEX CONFIGURATION (REQUIRED)
# =============================================================================
# Get these values by running: npx convex dev
# The deployment name and URL will be displayed in the terminal
CONVEX_DEPLOYMENT=dev:your-deployment-name
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# =============================================================================
# BETTER AUTH CONFIGURATION (REQUIRED)
# =============================================================================
# Generate a secure secret with: openssl rand -base64 32
# This is used to sign JWT tokens and encrypt session data
BETTER_AUTH_SECRET=

# Your application URL (change in production)
SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =============================================================================
# OPTIONAL: EMAIL PROVIDER (for password reset, verification)
# =============================================================================
# If you want to enable email verification or password reset:
# RESEND_API_KEY=
# Or use another email provider supported by Better Auth

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================
# 1. Copy this file to .env.local
# 2. Fill in the required values
# 3. Never commit .env.local to version control
# 4. Run: npx convex dev to get your Convex deployment values
# 5. Generate BETTER_AUTH_SECRET with: openssl rand -base64 32
